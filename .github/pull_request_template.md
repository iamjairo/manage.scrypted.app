## Summary

<!-- One or two sentences describing what this PR does and why. -->

## Area

<!-- Tick the area(s) this PR touches. Also add the matching `area:*` label. -->

- [ ] `src/` — Vue SPA (`area:frontend`)
- [ ] `infra/` — Docker Compose / Caddy (`area:infra`)
- [ ] `server-app/` — Electron launcher (`area:server-app`)
- [ ] `server-app-tauri/` — Tauri launcher (`area:server-app-tauri`)
- [ ] `agent-harness/` (`area:agent-harness`)
- [ ] `.github/` / CI (`area:ci`)
- [ ] Docs only (`area:docs`)

## Type

- [ ] Bug fix
- [ ] New feature
- [ ] Refactor / chore
- [ ] Documentation
- [ ] Dependency update

## Testing

<!-- How was this verified? Include manual test steps, screenshots, or
     command output where useful. For UI changes, attach a before/after
     screenshot. For infra changes, paste the relevant `docker compose`
     or `caddy validate` output. -->

## Upstream relationship

- [ ] This change should eventually be **proposed upstream** to `koush/manage.scrypted.app`.
- [ ] This change is **fork-only** (add the `fork-only` label).
- [ ] N/A (`infra/`, `server-app/`, etc. — not part of the upstream surface).

## Checklist

- [ ] I added the appropriate `area:*` label.
- [ ] CI is green (or I've explained why a failure is unrelated).
- [ ] If this touches user-facing strings or behavior, the README / sub-README is updated.
